name: native-ui
version: 0.1.0

authors:
  - WhoAteDaCake <doitsimplefy@gmail.com>

crystal: 1.0.0

scripts:
  postinstall: "cd LCUI && ./autogen.sh && ./configure --prefix=$(pwd) && make"

license: MIT
